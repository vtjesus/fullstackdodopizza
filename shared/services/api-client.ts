//В данном файле собираются воедино все запросы

import * as products from './products';
import * as ingedients from './ingredients';
import * as cart from './cart';

export const Api = {
    products,
    ingedients,
    cart,
}